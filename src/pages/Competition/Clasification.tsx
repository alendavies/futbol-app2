function Clasification({ compId }: { compId: string }) {
    return (
        <div className="space-y-6">
            <p className="text-white font-bold text-xl">Clasificaciones</p>
        </div>
    );
}

export default Clasification;
